/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package ai.shipeasy.admin.models


import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * Body for `PATCH /api/admin/projects/{id}`. Partial — every field is optional; only supplied fields change. The project id in the path must match the caller's own project.
 *
 * @param name New project name.
 * @param domain Lowercase bare hostname (e.g. `acme.com`, `app.acme.com`, `*.acme.com`), or `*` to allow any origin. Full URLs with `https://` are not accepted. The project is keyed by `(owner_email, domain)`, so a second call with the same domain returns the existing project.
 * @param slug URL-safe identifier used in app URLs and SDK config. Lowercase letters, numbers, and hyphens; 2–48 chars; cannot start or end with a hyphen. The caller lowercases the raw slug before sending.
 * @param defaultEnv Default environment new resources are scoped to.
 * @param timezone IANA timezone the project's daily analysis runs in.
 * @param statMethod Statistical method the experiment analyzer uses.
 * @param sigThreshold Significance threshold (alpha) for experiment analysis.
 * @param autoRollback Whether a failing guardrail auto-rolls back the experiment.
 * @param minSampleDays Minimum number of days an experiment must run before it can be called.
 * @param moduleTranslations Enable/disable the i18n/translations module.
 * @param moduleConfigs Enable/disable the dynamic-configs module.
 * @param moduleGates Enable/disable the feature-gates module.
 * @param moduleExperiments Enable/disable the experiments module.
 * @param moduleFeedback Enable/disable the feedback/ops module.
 * @param moduleUser Enable/disable the user-management module.
 * @param moduleEvents Enable/disable the events module.
 * @param minSampleSize Verdict power guard — minimum users per arm before a ship/hold verdict.
 * @param minRuntimeDays Minimum days an experiment must run before a verdict (peeking guard).
 * @param defaultPower Target statistical power (1−β) feeding the realized-MDE calculation.
 * @param ciConfidence Confidence level for the interval surfaced on results (any value in [0.5, 0.999], e.g. 0.90, 0.95, 0.975, 0.99).
 * @param defaultAllocationPct Default traffic allocation (basis points, 1000 = 10%) new experiments start with; overridable per experiment.
 * @param defaultWinsorizePct Default winsorization percentile new metrics start with; overridable per metric.
 * @param defaultMei Default minimum effect of interest (relative, 0–1) new metrics start with; overridable per metric and per experiment. Null clears it.
 * @param cupedBaselineDays CUPED baseline window — days of pre-experiment history, frozen at start.
 * @param cupedMinOverlap CUPED selection-bias guard — min share of users with a baseline, else skip.
 * @param cupedMinBaselineUsers CUPED — minimum users with a baseline before it runs at all.
 * @param msprtTauMeiFactor mSPRT prior width — τ = minimum effect of interest × this factor.
 * @param msprtTauSdFactor mSPRT fallback prior width — τ = this × control SD when no MEI is set.
 * @param srmThreshold SRM chi-square p-value below which the run is called invalid.
 */


data class UpdateProjectRequest (

    /* New project name. */
    @Json(name = "name")
    val name: kotlin.String? = null,

    /* Lowercase bare hostname (e.g. `acme.com`, `app.acme.com`, `*.acme.com`), or `*` to allow any origin. Full URLs with `https://` are not accepted. The project is keyed by `(owner_email, domain)`, so a second call with the same domain returns the existing project. */
    @Json(name = "domain")
    val domain: kotlin.String? = null,

    /* URL-safe identifier used in app URLs and SDK config. Lowercase letters, numbers, and hyphens; 2–48 chars; cannot start or end with a hyphen. The caller lowercases the raw slug before sending. */
    @Json(name = "slug")
    val slug: kotlin.String? = null,

    /* Default environment new resources are scoped to. */
    @Json(name = "defaultEnv")
    val defaultEnv: UpdateProjectRequest.DefaultEnv? = null,

    /* IANA timezone the project's daily analysis runs in. */
    @Json(name = "timezone")
    val timezone: kotlin.String? = null,

    /* Statistical method the experiment analyzer uses. */
    @Json(name = "statMethod")
    val statMethod: UpdateProjectRequest.StatMethod? = null,

    /* Significance threshold (alpha) for experiment analysis. */
    @Json(name = "sigThreshold")
    val sigThreshold: UpdateProjectRequest.SigThreshold? = null,

    /* Whether a failing guardrail auto-rolls back the experiment. */
    @Json(name = "autoRollback")
    val autoRollback: kotlin.Boolean? = null,

    /* Minimum number of days an experiment must run before it can be called. */
    @Json(name = "minSampleDays")
    val minSampleDays: kotlin.Int? = null,

    /* Enable/disable the i18n/translations module. */
    @Json(name = "moduleTranslations")
    val moduleTranslations: kotlin.Boolean? = null,

    /* Enable/disable the dynamic-configs module. */
    @Json(name = "moduleConfigs")
    val moduleConfigs: kotlin.Boolean? = null,

    /* Enable/disable the feature-gates module. */
    @Json(name = "moduleGates")
    val moduleGates: kotlin.Boolean? = null,

    /* Enable/disable the experiments module. */
    @Json(name = "moduleExperiments")
    val moduleExperiments: kotlin.Boolean? = null,

    /* Enable/disable the feedback/ops module. */
    @Json(name = "moduleFeedback")
    val moduleFeedback: kotlin.Boolean? = null,

    /* Enable/disable the user-management module. */
    @Json(name = "moduleUser")
    val moduleUser: kotlin.Boolean? = null,

    /* Enable/disable the events module. */
    @Json(name = "moduleEvents")
    val moduleEvents: kotlin.Boolean? = null,

    /* Verdict power guard — minimum users per arm before a ship/hold verdict. */
    @Json(name = "minSampleSize")
    val minSampleSize: kotlin.Int? = null,

    /* Minimum days an experiment must run before a verdict (peeking guard). */
    @Json(name = "minRuntimeDays")
    val minRuntimeDays: kotlin.Int? = null,

    /* Target statistical power (1−β) feeding the realized-MDE calculation. */
    @Json(name = "defaultPower")
    val defaultPower: java.math.BigDecimal? = null,

    /* Confidence level for the interval surfaced on results (any value in [0.5, 0.999], e.g. 0.90, 0.95, 0.975, 0.99). */
    @Json(name = "ciConfidence")
    val ciConfidence: java.math.BigDecimal? = null,

    /* Default traffic allocation (basis points, 1000 = 10%) new experiments start with; overridable per experiment. */
    @Json(name = "defaultAllocationPct")
    val defaultAllocationPct: kotlin.Int? = null,

    /* Default winsorization percentile new metrics start with; overridable per metric. */
    @Json(name = "defaultWinsorizePct")
    val defaultWinsorizePct: kotlin.Int? = null,

    /* Default minimum effect of interest (relative, 0–1) new metrics start with; overridable per metric and per experiment. Null clears it. */
    @Json(name = "defaultMei")
    val defaultMei: java.math.BigDecimal? = null,

    /* CUPED baseline window — days of pre-experiment history, frozen at start. */
    @Json(name = "cupedBaselineDays")
    val cupedBaselineDays: kotlin.Int? = null,

    /* CUPED selection-bias guard — min share of users with a baseline, else skip. */
    @Json(name = "cupedMinOverlap")
    val cupedMinOverlap: java.math.BigDecimal? = null,

    /* CUPED — minimum users with a baseline before it runs at all. */
    @Json(name = "cupedMinBaselineUsers")
    val cupedMinBaselineUsers: kotlin.Int? = null,

    /* mSPRT prior width — τ = minimum effect of interest × this factor. */
    @Json(name = "msprtTauMeiFactor")
    val msprtTauMeiFactor: java.math.BigDecimal? = null,

    /* mSPRT fallback prior width — τ = this × control SD when no MEI is set. */
    @Json(name = "msprtTauSdFactor")
    val msprtTauSdFactor: java.math.BigDecimal? = null,

    /* SRM chi-square p-value below which the run is called invalid. */
    @Json(name = "srmThreshold")
    val srmThreshold: java.math.BigDecimal? = null

) {

    /**
     * Default environment new resources are scoped to.
     *
     * Values: dev,staging,prod
     */
    @JsonClass(generateAdapter = false)
    enum class DefaultEnv(val value: kotlin.String) {
        @Json(name = "dev") dev("dev"),
        @Json(name = "staging") staging("staging"),
        @Json(name = "prod") prod("prod");
    }
    /**
     * Statistical method the experiment analyzer uses.
     *
     * Values: sequential,fixed,bayesian
     */
    @JsonClass(generateAdapter = false)
    enum class StatMethod(val value: kotlin.String) {
        @Json(name = "sequential") sequential("sequential"),
        @Json(name = "fixed") fixed("fixed"),
        @Json(name = "bayesian") bayesian("bayesian");
    }
    /**
     * Significance threshold (alpha) for experiment analysis.
     *
     * Values: _0Period01,_0Period05,_0Period10
     */
    @JsonClass(generateAdapter = false)
    enum class SigThreshold(val value: kotlin.String) {
        @Json(name = "0.01") _0Period01("0.01"),
        @Json(name = "0.05") _0Period05("0.05"),
        @Json(name = "0.10") _0Period10("0.10");
    }

}

