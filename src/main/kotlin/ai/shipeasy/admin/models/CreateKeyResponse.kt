/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package ai.shipeasy.admin.models


import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * The minted key. The plaintext `key` is returned **once**, here — it is stored hashed and can never be retrieved again.
 *
 * @param id Stable opaque key id (UUID). Use it to revoke the key.
 * @param type The kind of key that was minted.
 * @param env Environment the key is bound to (`prod` for `admin`/`ops`).
 * @param key The plaintext API token (e.g. `sdk_server_…`). Returned once — store it now; it cannot be recovered.
 * @param expiresAt ISO-8601 expiry, or `null` if the key never expires.
 */


data class CreateKeyResponse (

    /* Stable opaque key id (UUID). Use it to revoke the key. */
    @Json(name = "id")
    val id: kotlin.String,

    /* The kind of key that was minted. */
    @Json(name = "type")
    val type: CreateKeyResponse.Type,

    /* Environment the key is bound to (`prod` for `admin`/`ops`). */
    @Json(name = "env")
    val env: CreateKeyResponse.Env,

    /* The plaintext API token (e.g. `sdk_server_…`). Returned once — store it now; it cannot be recovered. */
    @Json(name = "key")
    val key: kotlin.String,

    /* ISO-8601 expiry, or `null` if the key never expires. */
    @Json(name = "expires_at")
    val expiresAt: kotlin.String?

) {

    /**
     * The kind of key that was minted.
     *
     * Values: server,client,admin,ops
     */
    @JsonClass(generateAdapter = false)
    enum class Type(val value: kotlin.String) {
        @Json(name = "server") server("server"),
        @Json(name = "client") client("client"),
        @Json(name = "admin") admin("admin"),
        @Json(name = "ops") ops("ops");
    }
    /**
     * Environment the key is bound to (`prod` for `admin`/`ops`).
     *
     * Values: dev,staging,prod
     */
    @JsonClass(generateAdapter = false)
    enum class Env(val value: kotlin.String) {
        @Json(name = "dev") dev("dev"),
        @Json(name = "staging") staging("staging"),
        @Json(name = "prod") prod("prod");
    }

}

