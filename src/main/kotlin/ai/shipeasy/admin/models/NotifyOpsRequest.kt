/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package ai.shipeasy.admin.models


import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * Body for `POST /api/admin/notifications`. In `title`, `summary`, and each step, reference admin entities inline with tokens — `#42` (feedback item), `@gate:<name>` (alias `@flag:`), `@experiment:<name>` (`@exp:`), `@config:<name>`, `@killswitch:<name>` (`@ks:`), `@metric:<name>`, `@universe:<name>`, `@alert:<name>` — where `<name>` is the entity's immutable name/slug (e.g. `features.checkout`). The dashboard renders each token as a live hover chip deep-linking to the entity; tokens degrade to plain text elsewhere, so they're always safe to use.
 *
 * @param title One-line headline of what's blocked.
 * @param summary One sentence: why it can't be fixed in code. Renders markdown.
 * @param steps Ordered steps the human should take to unblock — self-contained (the human reads only this card, not the agent's transcript), 3–6 steps, each naming the exact file, command, env var, or dashboard page. Renders markdown.
 * @param href Dashboard-relative deep link to the related item. `null` is accepted and treated as \"no link\".
 * @param dedupeKey Stable per-escalation key (e.g. `feedback:7`) so re-runs dedupe to one row.
 */


data class NotifyOpsRequest (

    /* One-line headline of what's blocked. */
    @Json(name = "title")
    val title: kotlin.String,

    /* One sentence: why it can't be fixed in code. Renders markdown. */
    @Json(name = "summary")
    val summary: kotlin.String,

    /* Ordered steps the human should take to unblock — self-contained (the human reads only this card, not the agent's transcript), 3–6 steps, each naming the exact file, command, env var, or dashboard page. Renders markdown. */
    @Json(name = "steps")
    val steps: kotlin.collections.List<kotlin.String>? = null,

    /* Dashboard-relative deep link to the related item. `null` is accepted and treated as \"no link\". */
    @Json(name = "href")
    val href: kotlin.String? = null,

    /* Stable per-escalation key (e.g. `feedback:7`) so re-runs dedupe to one row. */
    @Json(name = "dedupeKey")
    val dedupeKey: kotlin.String? = null

) {


}

