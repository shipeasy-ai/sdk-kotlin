/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package ai.shipeasy.admin.models


import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * A single API key as returned by the list endpoint. Response fields are snake_case. The raw token is never returned here — only its `last4` tail. `last4` and the typed `scopes` may be `null` for keys minted before those columns existed.
 *
 * @param id Stable opaque key id (UUID). Use it to revoke the key.
 * @param type Key kind. `server` (back-end, full SDK auth), `client` (public, browser-embeddable), `admin` (CLI/devtools auth token), `ops` (restricted unattended-trigger credential).
 * @param env Environment the key is bound to. The worker derives the read env from the key, so this is the isolation boundary. `admin`/`ops` keys are env-agnostic and pinned to `prod`.
 * @param createdAt ISO-8601 timestamp the key was minted.
 * @param revokedAt ISO-8601 timestamp the key was revoked, or `null` while the key is active.
 * @param expiresAt ISO-8601 expiry, or `null` for a key that never expires. `admin` keys are fixed at 90 days; `ops` keys use a short sliding window.
 * @param createdByEmail Email of the human or actor that minted the key, or `null` when not attributable (e.g. CLI device-flow).
 * @param name Human label for the key. `null` for unnamed dashboard mints; programmatic mints auto-name themselves.
 * @param scopes Permission strings recorded on the key for audit/display, or `null` when none were set.
 * @param last4 Last 4 characters of the raw token, so the dashboard mask is matchable against the held key. `null` for keys minted before this column.
 */


data class KeyRecord (

    /* Stable opaque key id (UUID). Use it to revoke the key. */
    @Json(name = "id")
    val id: kotlin.String,

    /* Key kind. `server` (back-end, full SDK auth), `client` (public, browser-embeddable), `admin` (CLI/devtools auth token), `ops` (restricted unattended-trigger credential). */
    @Json(name = "type")
    val type: KeyRecord.Type,

    /* Environment the key is bound to. The worker derives the read env from the key, so this is the isolation boundary. `admin`/`ops` keys are env-agnostic and pinned to `prod`. */
    @Json(name = "env")
    val env: KeyRecord.Env,

    /* ISO-8601 timestamp the key was minted. */
    @Json(name = "created_at")
    val createdAt: kotlin.String,

    /* ISO-8601 timestamp the key was revoked, or `null` while the key is active. */
    @Json(name = "revoked_at")
    val revokedAt: kotlin.String?,

    /* ISO-8601 expiry, or `null` for a key that never expires. `admin` keys are fixed at 90 days; `ops` keys use a short sliding window. */
    @Json(name = "expires_at")
    val expiresAt: kotlin.String?,

    /* Email of the human or actor that minted the key, or `null` when not attributable (e.g. CLI device-flow). */
    @Json(name = "created_by_email")
    val createdByEmail: kotlin.String?,

    /* Human label for the key. `null` for unnamed dashboard mints; programmatic mints auto-name themselves. */
    @Json(name = "name")
    val name: kotlin.String?,

    /* Permission strings recorded on the key for audit/display, or `null` when none were set. */
    @Json(name = "scopes")
    val scopes: kotlin.collections.List<kotlin.String>?,

    /* Last 4 characters of the raw token, so the dashboard mask is matchable against the held key. `null` for keys minted before this column. */
    @Json(name = "last4")
    val last4: kotlin.String?

) {

    /**
     * Key kind. `server` (back-end, full SDK auth), `client` (public, browser-embeddable), `admin` (CLI/devtools auth token), `ops` (restricted unattended-trigger credential).
     *
     * Values: server,client,admin,ops
     */
    @JsonClass(generateAdapter = false)
    enum class Type(val value: kotlin.String) {
        @Json(name = "server") server("server"),
        @Json(name = "client") client("client"),
        @Json(name = "admin") admin("admin"),
        @Json(name = "ops") ops("ops");
    }
    /**
     * Environment the key is bound to. The worker derives the read env from the key, so this is the isolation boundary. `admin`/`ops` keys are env-agnostic and pinned to `prod`.
     *
     * Values: dev,staging,prod
     */
    @JsonClass(generateAdapter = false)
    enum class Env(val value: kotlin.String) {
        @Json(name = "dev") dev("dev"),
        @Json(name = "staging") staging("staging"),
        @Json(name = "prod") prod("prod");
    }

}

