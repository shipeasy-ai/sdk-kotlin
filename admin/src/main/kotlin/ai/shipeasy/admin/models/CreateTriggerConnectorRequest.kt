/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package ai.shipeasy.admin.models

import ai.shipeasy.admin.models.ConnectorEvent
import ai.shipeasy.admin.models.CreateClaudeTriggerRequest
import ai.shipeasy.admin.models.CreateCopilotTriggerRequest
import ai.shipeasy.admin.models.CreateCursorTriggerRequest
import ai.shipeasy.admin.models.CreateJulesTriggerRequest
import ai.shipeasy.admin.models.JulesTriggerConfig

import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * Body for `POST /api/admin/connectors/trigger`, discriminated on `provider` — the trigger-provider subset of `CreateConnectorRequest` (no OAuth stubs). Config + credentials arrive together and the connector is usable immediately; creates are idempotent by each provider's natural key (`config.routineId` / `config.repoUrl` / `config.owner`+`config.repo` / `config.source`).
 *
 * @param provider Discriminator. Google Jules trigger.
 * @param config 
 * @param token Copilot-licensed user PAT (secret). The ops key lives in the repo's GitHub \"Agents\" secret store and is never sent through Shipeasy. Encrypted; never returned.
 * @param apiKey Jules API key that launches the session (secret). Encrypted into the credentials cipher; never returned.
 * @param opsKey Restricted Shipeasy ops key, embedded in the prompt (Jules exposes no env channel) (secret). Encrypted; never returned.
 * @param name Human-readable connector label.
 * @param events Events that auto-fire a Jules session. Defaults to empty.
 * @param enabled Whether the trigger is active on create.
 */


interface CreateTriggerConnectorRequest {

    /* Discriminator. Google Jules trigger. */
    @Json(name = "provider")
    val provider: CreateTriggerConnectorRequest.Provider
    @Json(name = "config")
    val config: JulesTriggerConfig
    /* Copilot-licensed user PAT (secret). The ops key lives in the repo's GitHub \"Agents\" secret store and is never sent through Shipeasy. Encrypted; never returned. */
    @Json(name = "token")
    val token: kotlin.String
    /* Jules API key that launches the session (secret). Encrypted into the credentials cipher; never returned. */
    @Json(name = "apiKey")
    val apiKey: kotlin.String
    /* Restricted Shipeasy ops key, embedded in the prompt (Jules exposes no env channel) (secret). Encrypted; never returned. */
    @Json(name = "opsKey")
    val opsKey: kotlin.String
    /* Human-readable connector label. */
    @Json(name = "name")
    val name: kotlin.String?
    /* Events that auto-fire a Jules session. Defaults to empty. */
    @Json(name = "events")
    val events: kotlin.collections.List<ConnectorEvent>?
    /* Whether the trigger is active on create. */
    @Json(name = "enabled")
    val enabled: kotlin.Boolean?
    /**
     * Discriminator. Google Jules trigger.
     *
     * Values: jules_trigger
     */
    @JsonClass(generateAdapter = false)
    enum class Provider(val value: kotlin.String) {
        @Json(name = "jules_trigger") jules_trigger("jules_trigger");
    }

}

