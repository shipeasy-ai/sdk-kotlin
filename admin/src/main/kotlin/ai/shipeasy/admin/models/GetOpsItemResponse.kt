/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package ai.shipeasy.admin.models

import ai.shipeasy.admin.models.ConnectorData
import ai.shipeasy.admin.models.NotificationTarget
import ai.shipeasy.admin.models.OpsItemAttachment
import ai.shipeasy.admin.models.OpsItemContext
import ai.shipeasy.admin.models.OpsItemPriority
import ai.shipeasy.admin.models.OpsItemRelated
import ai.shipeasy.admin.models.OpsItemStatus

import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * One queue item, any of the five types. Shared fields apply to all; `stepsToReproduce`/`actualResult`/`expectedResult` are bug-specific, `description`/`useCase` feature-specific. The auto-collected browser fields (page URL, user-agent, viewport) live under `context.browser` for bug/feature. `context` also carries the hydrated per-type payload for auto-filed `error`/`alert`/`measure_plan` tickets, `attachments` lists any uploaded files, and `related` gives deep links to the underlying resources.
 *
 * @param id Stable opaque item id.
 * @param number Per-project item number (the `#7` handle), or `null` if unnumbered.
 * @param type Queue item type. `bug` and `feature_request` are user-fileable; `error`, `alert`, and `measure_plan` tickets are auto-filed by the platform (a tracked production error, a metric-threshold alert, and an assistant-proposed measurement plan respectively).
 * @param title One-line item title (all types).
 * @param status 
 * @param priority Triage priority, or `null` if not yet set (all types).
 * @param context Type-specific capture context. For `bug`/`feature_request` it carries `browser` (the auto-collected page URL / user-agent / viewport). For auto-filed tickets it carries the hydrated `error`, `alert`, or `measurePlan` block. `null` only for legacy rows with nothing captured.
 * @param attachments Files attached to the report — screenshots, recordings, logs. Populated for `bug`/`feature_request`; always an empty array for auto-filed `error`/`alert`/`measure_plan` tickets. Fetch each via its `fetchUrl` with the same admin/ops key (the download route is ops-key allow-listed).
 * @param related 
 * @param createdAt ISO-8601 creation timestamp.
 * @param source How the item was filed: `team` (a human admin/teammate — bug/feature) or `system` (auto-filed — error/alert/measure_plan).
 * @param sourceRef Stable key of the originating record for auto-filed tickets (error fingerprint, `<alert source>:<dedupeKey>`, or measurement-plan ref); `null` for human-filed bugs/features.
 * @param reporterEmail Reporter email (bug/feature), or `null`.
 * @param stepsToReproduce Reproduction steps — populated for `bug`, empty string otherwise.
 * @param actualResult What actually happened — `bug` only, empty string otherwise.
 * @param expectedResult What was expected — `bug` only, empty string otherwise.
 * @param description Feature description — populated for `feature_request`, empty string otherwise.
 * @param useCase Feature use case — `feature_request` only, empty string otherwise.
 * @param connectorData Per-connector linkage recorded for this item, keyed by connector provider. Populated as connectors act on the item (e.g. GitHub opens an issue, Slack posts a message); more than one provider can be present at once. `null` if no connector has touched the item.
 * @param notify Per-item completion-notification target, or `null` (falls back to the project default).
 * @param updatedAt ISO-8601 last-update timestamp.
 */


data class GetOpsItemResponse (

    /* Stable opaque item id. */
    @Json(name = "id")
    val id: kotlin.String,

    /* Per-project item number (the `#7` handle), or `null` if unnumbered. */
    @Json(name = "number")
    val number: java.math.BigDecimal?,

    /* Queue item type. `bug` and `feature_request` are user-fileable; `error`, `alert`, and `measure_plan` tickets are auto-filed by the platform (a tracked production error, a metric-threshold alert, and an assistant-proposed measurement plan respectively). */
    @Json(name = "type")
    val type: GetOpsItemResponse.Type,

    /* One-line item title (all types). */
    @Json(name = "title")
    val title: kotlin.String,

    @Json(name = "status")
    val status: OpsItemStatus,

    /* Triage priority, or `null` if not yet set (all types). */
    @Json(name = "priority")
    val priority: OpsItemPriority?,

    /* Type-specific capture context. For `bug`/`feature_request` it carries `browser` (the auto-collected page URL / user-agent / viewport). For auto-filed tickets it carries the hydrated `error`, `alert`, or `measurePlan` block. `null` only for legacy rows with nothing captured. */
    @Json(name = "context")
    val context: OpsItemContext?,

    /* Files attached to the report — screenshots, recordings, logs. Populated for `bug`/`feature_request`; always an empty array for auto-filed `error`/`alert`/`measure_plan` tickets. Fetch each via its `fetchUrl` with the same admin/ops key (the download route is ops-key allow-listed). */
    @Json(name = "attachments")
    val attachments: kotlin.collections.List<OpsItemAttachment>,

    @Json(name = "related")
    val related: OpsItemRelated,

    /* ISO-8601 creation timestamp. */
    @Json(name = "createdAt")
    val createdAt: kotlin.String,

    /* How the item was filed: `team` (a human admin/teammate — bug/feature) or `system` (auto-filed — error/alert/measure_plan). */
    @Json(name = "source")
    val source: GetOpsItemResponse.Source? = null,

    /* Stable key of the originating record for auto-filed tickets (error fingerprint, `<alert source>:<dedupeKey>`, or measurement-plan ref); `null` for human-filed bugs/features. */
    @Json(name = "sourceRef")
    val sourceRef: kotlin.String? = null,

    /* Reporter email (bug/feature), or `null`. */
    @Json(name = "reporterEmail")
    val reporterEmail: kotlin.String? = null,

    /* Reproduction steps — populated for `bug`, empty string otherwise. */
    @Json(name = "stepsToReproduce")
    val stepsToReproduce: kotlin.String? = null,

    /* What actually happened — `bug` only, empty string otherwise. */
    @Json(name = "actualResult")
    val actualResult: kotlin.String? = null,

    /* What was expected — `bug` only, empty string otherwise. */
    @Json(name = "expectedResult")
    val expectedResult: kotlin.String? = null,

    /* Feature description — populated for `feature_request`, empty string otherwise. */
    @Json(name = "description")
    val description: kotlin.String? = null,

    /* Feature use case — `feature_request` only, empty string otherwise. */
    @Json(name = "useCase")
    val useCase: kotlin.String? = null,

    /* Per-connector linkage recorded for this item, keyed by connector provider. Populated as connectors act on the item (e.g. GitHub opens an issue, Slack posts a message); more than one provider can be present at once. `null` if no connector has touched the item. */
    @Json(name = "connectorData")
    val connectorData: ConnectorData? = null,

    /* Per-item completion-notification target, or `null` (falls back to the project default). */
    @Json(name = "notify")
    val notify: NotificationTarget? = null,

    /* ISO-8601 last-update timestamp. */
    @Json(name = "updatedAt")
    val updatedAt: kotlin.String? = null

) : kotlin.collections.HashMap<String, kotlin.Any>() {

    /**
     * Queue item type. `bug` and `feature_request` are user-fileable; `error`, `alert`, and `measure_plan` tickets are auto-filed by the platform (a tracked production error, a metric-threshold alert, and an assistant-proposed measurement plan respectively).
     *
     * Values: bug,feature_request,error,alert,measure_plan
     */
    @JsonClass(generateAdapter = false)
    enum class Type(val value: kotlin.String) {
        @Json(name = "bug") bug("bug"),
        @Json(name = "feature_request") feature_request("feature_request"),
        @Json(name = "error") error("error"),
        @Json(name = "alert") alert("alert"),
        @Json(name = "measure_plan") measure_plan("measure_plan");
    }
    /**
     * How the item was filed: `team` (a human admin/teammate — bug/feature) or `system` (auto-filed — error/alert/measure_plan).
     *
     * Values: team,system
     */
    @JsonClass(generateAdapter = false)
    enum class Source(val value: kotlin.String) {
        @Json(name = "team") team("team"),
        @Json(name = "system") system("system");
    }

}

