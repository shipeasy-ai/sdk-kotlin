/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package ai.shipeasy.admin.models

import ai.shipeasy.admin.models.ConnectorEvent
import ai.shipeasy.admin.models.ConnectorProvider

import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * A connector row. The encrypted credentials cipher backing the connector is intentionally never serialised.
 *
 * @param id Stable opaque connector id (a UUID).
 * @param projectId Id of the project the connector belongs to.
 * @param provider 
 * @param name Human-readable connector label shown in the dashboard.
 * @param enabled Whether the connector is active. OAuth providers are created `false` and flip to `true` only after the OAuth/config flow completes; trigger providers default to `true` on create. A disabled connector never dispatches and never auto-fires.
 * @param events Events this connector is subscribed to. Empty array = no auto-dispatch / no auto-fire (the connector can still be fired/tested manually).
 * @param config Provider-specific, non-secret configuration (e.g. a Google Sheets `spreadsheetId`/`sheetTitle`, a Slack channel ref, or a trigger's repo coordinates + routine id). Secrets are never stored here — they live in an encrypted credentials cipher that is never returned by the API.
 * @param accountLabel Display label for the connected account / target (e.g. the OAuth account email, or a trigger's idempotency key such as its repo url or routine id). `null` until the connector is authenticated/configured.
 * @param lastError Error message from the most recent failed dispatch/fire attempt, or `null` if the last attempt succeeded (or none has run).
 * @param lastAttemptAt ISO-8601 timestamp of the most recent dispatch/fire attempt, or `null` if none has run.
 * @param lastSuccessAt ISO-8601 timestamp of the most recent successful dispatch/fire, or `null` if none has succeeded. Preserved across later failures.
 * @param createdAt ISO-8601 timestamp of creation.
 * @param updatedAt ISO-8601 timestamp of last mutation.
 */


data class ConnectorRecord (

    /* Stable opaque connector id (a UUID). */
    @Json(name = "id")
    val id: kotlin.String,

    /* Id of the project the connector belongs to. */
    @Json(name = "projectId")
    val projectId: kotlin.String,

    @Json(name = "provider")
    val provider: ConnectorProvider,

    /* Human-readable connector label shown in the dashboard. */
    @Json(name = "name")
    val name: kotlin.String,

    /* Whether the connector is active. OAuth providers are created `false` and flip to `true` only after the OAuth/config flow completes; trigger providers default to `true` on create. A disabled connector never dispatches and never auto-fires. */
    @Json(name = "enabled")
    val enabled: kotlin.Boolean,

    /* Events this connector is subscribed to. Empty array = no auto-dispatch / no auto-fire (the connector can still be fired/tested manually). */
    @Json(name = "events")
    val events: kotlin.collections.List<ConnectorEvent>,

    /* Provider-specific, non-secret configuration (e.g. a Google Sheets `spreadsheetId`/`sheetTitle`, a Slack channel ref, or a trigger's repo coordinates + routine id). Secrets are never stored here — they live in an encrypted credentials cipher that is never returned by the API. */
    @Json(name = "config")
    val config: kotlin.collections.Map<kotlin.String, kotlin.Any>,

    /* Display label for the connected account / target (e.g. the OAuth account email, or a trigger's idempotency key such as its repo url or routine id). `null` until the connector is authenticated/configured. */
    @Json(name = "accountLabel")
    val accountLabel: kotlin.String?,

    /* Error message from the most recent failed dispatch/fire attempt, or `null` if the last attempt succeeded (or none has run). */
    @Json(name = "lastError")
    val lastError: kotlin.String?,

    /* ISO-8601 timestamp of the most recent dispatch/fire attempt, or `null` if none has run. */
    @Json(name = "lastAttemptAt")
    val lastAttemptAt: kotlin.String?,

    /* ISO-8601 timestamp of the most recent successful dispatch/fire, or `null` if none has succeeded. Preserved across later failures. */
    @Json(name = "lastSuccessAt")
    val lastSuccessAt: kotlin.String?,

    /* ISO-8601 timestamp of creation. */
    @Json(name = "createdAt")
    val createdAt: kotlin.String,

    /* ISO-8601 timestamp of last mutation. */
    @Json(name = "updatedAt")
    val updatedAt: kotlin.String

) {


}

