/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package ai.shipeasy.admin.models

import ai.shipeasy.admin.models.ClaudeTriggerConfig
import ai.shipeasy.admin.models.ConnectorEvent

import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * Create a Claude Code routine trigger in one shot. Idempotent by `config.routineId` — re-creating with the same routine id updates the existing row.
 *
 * @param provider Discriminator. Claude Code scheduled-routine trigger.
 * @param config 
 * @param name Human-readable connector label.
 * @param events Events that auto-fire the routine. Defaults to empty so the trigger does not auto-fire paid runs until events are subscribed.
 * @param token The routine's fire bearer token (secret). **Optional** — a tokenless trigger is recorded but not fireable until a token is added later. Encrypted into the credentials cipher; never persisted in `config` or returned.
 * @param enabled Whether the trigger is active on create.
 */


data class CreateClaudeTriggerRequest (

    /* Discriminator. Claude Code scheduled-routine trigger. */
    @Json(name = "provider")
    val provider: CreateClaudeTriggerRequest.Provider,

    @Json(name = "config")
    val config: ClaudeTriggerConfig,

    /* Human-readable connector label. */
    @Json(name = "name")
    val name: kotlin.String? = "Claude trigger",

    /* Events that auto-fire the routine. Defaults to empty so the trigger does not auto-fire paid runs until events are subscribed. */
    @Json(name = "events")
    val events: kotlin.collections.List<ConnectorEvent>? = null,

    /* The routine's fire bearer token (secret). **Optional** — a tokenless trigger is recorded but not fireable until a token is added later. Encrypted into the credentials cipher; never persisted in `config` or returned. */
    @Json(name = "token")
    val token: kotlin.String? = null,

    /* Whether the trigger is active on create. */
    @Json(name = "enabled")
    val enabled: kotlin.Boolean? = true

) {

    /**
     * Discriminator. Claude Code scheduled-routine trigger.
     *
     * Values: claude_trigger
     */
    @JsonClass(generateAdapter = false)
    enum class Provider(val value: kotlin.String) {
        @Json(name = "claude_trigger") claude_trigger("claude_trigger");
    }

}

