/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "DuplicatedCode",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "RemoveRedundantCallsOfConversionMethods",
    "REDUNDANT_CALL_OF_CONVERSION_METHOD",
    "RedundantUnitReturnType",
    "RemoveEmptyClassBody",
    "UnnecessaryVariable",
    "UnusedImport",
    "UnnecessaryVariable",
    "unused"
)

package ai.shipeasy.admin.models


import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * 
 *
 * @param metricId Existing metric id in the project.
 * @param role Metric role. `goal` drives the decision, `guardrail` blocks ship if degraded, `secondary` is informational.
 * @param minEffectOfInterest Per-experiment override of the metric's `default_min_effect_of_interest` (relative, 0–1) — the smallest change worth acting on for THIS experiment's decision, which depends on the intervention's cost/risk. `null`/omitted falls back to the metric default. For a `guardrail` this is the non-inferiority margin (how large a regression is tolerated); for a `goal` it is the superiority threshold.
 */


data class SetExperimentMetricsRequestMetricsInner (

    /* Existing metric id in the project. */
    @Json(name = "metric_id")
    val metricId: kotlin.String,

    /* Metric role. `goal` drives the decision, `guardrail` blocks ship if degraded, `secondary` is informational. */
    @Json(name = "role")
    val role: SetExperimentMetricsRequestMetricsInner.Role,

    /* Per-experiment override of the metric's `default_min_effect_of_interest` (relative, 0–1) — the smallest change worth acting on for THIS experiment's decision, which depends on the intervention's cost/risk. `null`/omitted falls back to the metric default. For a `guardrail` this is the non-inferiority margin (how large a regression is tolerated); for a `goal` it is the superiority threshold. */
    @Json(name = "min_effect_of_interest")
    val minEffectOfInterest: java.math.BigDecimal? = null

) {

    /**
     * Metric role. `goal` drives the decision, `guardrail` blocks ship if degraded, `secondary` is informational.
     *
     * Values: goal,guardrail,secondary
     */
    @JsonClass(generateAdapter = false)
    enum class Role(val value: kotlin.String) {
        @Json(name = "goal") goal("goal"),
        @Json(name = "guardrail") guardrail("guardrail"),
        @Json(name = "secondary") secondary("secondary");
    }

}

