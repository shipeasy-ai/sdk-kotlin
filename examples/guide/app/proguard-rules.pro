# Add project specific ProGuard rules here.
# The default rules from getDefaultProguardFile("proguard-android-optimize.txt")
# already cover AndroidX + Compose for this sample. Nothing extra is needed.
